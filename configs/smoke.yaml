seed: 42
data:
  batch_size: 8
model:
  feature_dim: 5
  encoder_width: 16
  encoder_depth: 2
  hyper_width: 12
  hyper_depth: 2
  hyper_dropout: 0.0
  state_dim: 6
  parameter_dim: 22
training:
  epochs: 2
  learning_rate: 0.001
  weight_decay: 0.0001
  collocation_points: 8

